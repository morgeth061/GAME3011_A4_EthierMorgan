using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class A4NodeBehaviour : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void OnNodeClick()
    {
        
    }

    private void RotateTile()
    {
        //Lock input
        //Rotate Clockwise
        //Check neighboring tiles with raycast
        //Check for circuit
        //IF powered circuit, THEN update tile colour
        //IF powered circuit, check raycast for matching neighbour
        //IF matching neighbour, power neighbour and check neighbour's neighbours
        //Check for win
        //Unlock Input
    }
}
